import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <Link to='/' className="text-secColor font-roboto font-bold text-[1.5rem] cursor-pointer">
      Printify
    </Link>
  )
}

export default Logo;
